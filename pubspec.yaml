name: flovix_kitchen
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2
  another_flushbar: ^1.12.30
  #auto_size_text: ^3.0.0
  dotted_line: ^3.2.3
  sizer: ^3.0.0
  qr_flutter: ^4.1.0
  connectivity_plus: ^6.1.5
  zatca_fatoora_flutter: ^1.0.0
  #hexcolor: ^3.0.1
  flutter_slidable: ^4.0.3
  shared_preferences: ^2.2.3
  email_validator: ^3.0.0
  http: ^1.2.1
  logger: ^1.4.0
  flutter_bloc: ^8.1.6
  bloc: ^8.1.4
  network_info_plus: ^4.1.0
  get_it: ^8.0.2
  cached_network_image: ^3.3.1


  equatable: ^2.0.5
  flutter_secure_storage: ^9.2.2
  freezed_annotation: ^2.4.3
  json_annotation: ^4.9.0
  freezed: ^2.5.2
  # printing: ^5.13.3
  flutter_screenutil: ^5.9.3
  esc_pos_utils_plus: 2.0.4
  image: ^4.5.4
  #  usb_serial:
  sunmi_printer_plus: ^4.1.1
  intl_mobile_field: ^2.1.4
  firebase_core: ^4.1.0
  firebase_messaging: ^16.0.1
  firebase_auth: ^6.0.2
  flutter_local_notifications: ^19.4.2
  #sunmi_printer_plus: ^3.0.0
  #barcode_widget: ^2.0.4
  #world_countries: ^3.0.0

  google_places_flutter: ^2.1.1
  google_maps_flutter: ^2.14.0
  easy_localization: ^3.0.7+1
  another_imin_printer: ^0.7.1
  package_info_plus: ^8.3.0
  url_launcher: ^6.3.2
  fl_chart: ^0.69.0
  intl: any
  sqflite: ^2.4.2
  sqflite_common_ffi: ^2.3.5
  sqlite3: ^2.9.4
  #thermal_printer: ^1.0.5
  flutter_thermal_printer: ^1.2.4
  #  flutter_thermal_printer: ^1.2.2
  permission_handler: ^11.3.1
  path_provider: ^2.1.4
  pdf: ^3.11.3
  printing: ^5.14.2
  screenshot: ^3.0.0
  widget_to_image_converter: ^1.1.1
  #dartarabic: ^0.3.1
  #  flutter_html: ^3.0.0
  #  flutter_bluetooth_serial_ble: ^0.5.0
  path: ^1.9.0
  open_file: ^3.3.1
  #bidi: ^2.0.13
  arabic_numbers: ^1.0.0
  webview_flutter_plus: ^0.4.19
  chucker_flutter: ^1.9.1
  qr_code_scanner_plus: ^2.0.12
  shimmer: ^3.0.0
  googleapis_auth: ^2.0.0
  sqflite_common_ffi_web: ^1.0.2
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.4.11
  json_serializable: ^6.8.0
  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg
  assets:
    - assets/images/
    - assets/icons/
    - assets/images/settings/
    - assets/translations/

  fonts:
    - family: Inter
      fonts:
        - asset: assets/fonts/inter/Inter-Thin-BETA.otf
          weight: 100
        - asset: assets/fonts/inter/Inter-ThinItalic-BETA.otf
          weight: 100
          style: italic
        - asset: assets/fonts/inter/Inter-ExtraLight-BETA.otf
          weight: 200
        - asset: assets/fonts/inter/Inter-ExtraLightItalic-BETA.otf
          weight: 200
          style: italic
        - asset: assets/fonts/inter/Inter-Light-BETA.otf
          weight: 300
        - asset: assets/fonts/inter/Inter-LightItalic-BETA.otf
          weight: 300
          style: italic
        - asset: assets/fonts/inter/Inter-Regular.otf
          weight: 400
        - asset: assets/fonts/inter/Inter-Italic.otf
          weight: 400
          style: italic
        - asset: assets/fonts/inter/Inter-Medium.otf
          weight: 500
        - asset: assets/fonts/inter/Inter-MediumItalic.otf
          weight: 500
          style: italic
        - asset: assets/fonts/inter/Inter-SemiBold.otf
          weight: 600
        - asset: assets/fonts/inter/Inter-SemiBoldItalic.otf
          weight: 600
          style: italic
        - asset: assets/fonts/inter/Inter-Bold.otf
          weight: 700
        - asset: assets/fonts/inter/Inter-BoldItalic.otf
          weight: 700
          style: italic
        - asset: assets/fonts/inter/Inter-ExtraBold.otf
          weight: 800
        - asset: assets/fonts/inter/Inter-ExtraBoldItalic.otf
          weight: 800
          style: italic
        - asset: assets/fonts/inter/Inter-Black.otf
          weight: 900
        - asset: assets/fonts/inter/Inter-BlackItalic.otf
          weight: 900
          style: italic
